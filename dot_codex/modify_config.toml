{{- /* chezmoi:modify-template */ -}}
{{- $config := fromToml (default "" .chezmoi.stdin) -}}
{{- $config = mergeOverwrite $config (dict "mcp_servers" (.mcpServers | default dict)) -}}
{{- $config | toToml -}}
