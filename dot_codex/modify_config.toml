{{- /* chezmoi:modify-template */ -}}
{{- $config := fromToml (default "" .chezmoi.stdin) -}}
{{- $config = mergeOverwrite $config (dict "project_doc_fallback_filenames" (list "CLAUDE.md")) -}}
{{- $config | toToml -}}
