{{- /* chezmoi:modify-template */ -}}
{{- $config := fromJson (default "{}" .chezmoi.stdin) -}}
{{-
  $config := $config | setValueAtPath "mcpServers" (
    $config.mcpServers | default dict | mergeOverwrite .mcpServers
  )
-}}
{{- $config | toPrettyJson -}}
