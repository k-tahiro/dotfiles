{{- /* chezmoi:modify-template */ -}}
{{
  fromJson (default "{}" .chezmoi.stdin)
  | setValueAtPath "mcpServers" (
    ((fromJson (default "{}" .chezmoi.stdin)).mcpServers | default dict)
    | mergeOverwrite .mcpServers
  )
  | toPrettyJson
}}
