#!/bin/bash
set -euo pipefail

BREW="{{- .brewPrefix -}}/bin/brew"

if [[ ! -x "$BREW" ]]; then
    echo "Error: Homebrew not found at $BREW"
    exit 1
fi

eval "$("$BREW" shellenv)"
"$BREW" update
"$BREW" upgrade -y
